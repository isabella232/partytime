import com.favoritemedium.partytime.domain.*

class PtMailServiceTests extends GroovyTestCase {

/*    void testSendRecoveryEmail() {
       assertEquals(0, wiser.messages.size())
       sendRecoveryEmail(user)
       assertEquals(1, wiser.messages.size())
    }
    void setUp() {
        user = new User(username:"test", password:'test123')
        mockDomain(User, [user])
        wiser.start()
    }

    void tearDown() {
        wiser.stop()
        User.list().each {
            it.delete(flush:true)
        }
    }
*/
}
